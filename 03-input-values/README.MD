# .value DOM Method in JavaScript

## What is `.value` ?

`.value` is a DOM property used to:

- Get the value from an input field
- Set a value inside an input field

It is mostly used with:

- input
- textarea
- select

elements.

---

# Basic Syntax

```js
document.getElementById("idName").value